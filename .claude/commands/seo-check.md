Vérifie les bonnes pratiques SEO pour la page $ARGUMENTS :

1. **Metadata** : Présence de `generateMetadata` ou `metadata` export
2. **Structured Data** : JSON-LD (Organization, WebSite, ou schéma spécifique)
3. **HTML sémantique** : Utilisation correcte des balises (h1, h2, article, section, nav)
4. **Images** : Attributs alt présents et descriptifs
5. **Liens** : Pas de liens cassés, attributs rel appropriés
6. **Performance** : Pas de composants client inutiles qui bloqueraient le SSR

Réfère-toi à .claude/routing.md pour les conventions de métadonnées du projet.
Fournis un rapport avec les améliorations suggérées.
