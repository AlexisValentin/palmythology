Lance les tests pour le composant $ARGUMENTS avec `pnpm test`.

1. Identifie les fichiers de test associés au composant
2. Lance les tests ciblés
3. Si des tests échouent, analyse les erreurs et propose des corrections
4. Si aucun test n'existe, propose d'en créer en suivant les patterns de .claude/components.md (Vitest + Testing Library, fichier .test.tsx colocalisé)
